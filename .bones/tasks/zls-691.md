---
id: zls-691
title: 'Phase 1 Acceptance: Cross-File Reference Coverage'
status: open
type: task
priority: 1
parent: zls-h4v
---



## Context

Acceptance task for Phase 1 (zls-h4v). The implementation task (zls-91m) is complete: eager transitive import loading + on-demand fallback are in place. This task verifies the implementation delivers real cross-file coverage improvement and presents the results.

## Deliverables

### Agent Documentation
- [ ] CLAUDE.md: none expected (no new commands or config)
- [ ] Project docs: none expected

### User Demo

Live narrated LSP tool walkthrough (from sub-epic zls-h4v Acceptance Requirements):

1. Run `findReferences` on `resolveTypeOfNode` (analysis.zig:1943) — show results now include references.zig, semantic_tokens.zig, inlay_hints.zig (previously missed)
2. Run `findReferences` on `getPositionContext` (analysis.zig:5355) — show it now finds references.zig and code_actions.zig (previously missed)
3. Explain each LSP call as it happens: what's being queried, what the result means, how many files were found vs the prior baseline
4. Show at least one case where a file that was previously missed is now found

### Sub-epic Criteria to Verify
- [ ] `findReferences` returns results across all transitively imported files (not just already-opened files)
- [ ] Live LSP demo: `findReferences` on `resolveTypeOfNode` finds callers in 5+ feature files

## Success Criteria
- [ ] Demo presented to user in conversation
- [ ] Both sub-epic criteria checked off
- [ ] User closes this task (agent does not self-close acceptance tasks)
